# Hooks

Place hook definitions or hook-adjacent documentation here when the harness needs runtime enforcement.

If the plugin uses Codex hook configuration, add the hook manifest expected by the target runtime and update `.codex-plugin/plugin.json` accordingly.
